func scores100(l: List[int], prev: int, curr: int) -> bool = {
	if (curr == size(l))
		false
	else if (l[curr] == 100 && prev == 100)
		true
	else
		scores100(l, l[curr], curr + 1)
};

val nums : List[int] = new List { 100, 1, 100, 100, 99, 100 };

if (scores100(nums, nums[0], 1))
	2
else
	1
