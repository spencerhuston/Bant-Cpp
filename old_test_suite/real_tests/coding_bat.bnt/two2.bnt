val l : List[int] = filter(map(new List { 1, 2, 3 }, 
				func d(x: int) -> int = x * 2; d), 
				func no2(x: int) -> bool = {
					if (x > 10)
						no2(x % 10)
					else
						x != 2
				}; no2);

if (size(l) == 2 && l[0] == 4 && l[1] == 6)
	2
else
	1
