val arr : List[int] = map(new List { 1, 2, 3, 4, 5, 6 },
				func f(x: int) -> int -> int = {
					func g(y: int) -> int = {
						if (x % 2 == 0) 
							x / y 
						else 
							x * y
					};
					g
				};
				f(2)
			);

if (front(arr) == 2 && arr[1] == 1 && size(arr) == 6)
	2
else
	1
