func no14(size: int, curr: int, arr: List[int]) -> bool = {
	if (size == curr)
		true
	else if ((arr[curr] == 1) || (arr[curr] == 4)) 
		false
	else
		no14(size, curr + 1, arr)
};

val b : bool = no14(10, 0, new List {2,3,5,6,7,4,8,5,6,7});

if (b)
	4
else
	2
