func f(x: List[int], y: (int, int) -> int) -> int = {
	y(x[0], x[1])
};

func g(a: int, b: int) -> int = {
	a + b
};

func h(a: int, b: int) -> int = {
	a * b
};

val arr : List[int] = new List { 1, 2 };
val ans : int = f(arr, g) + f(arr, h);
ans
