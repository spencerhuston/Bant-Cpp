val arr : List[int] = new List { 1, 2, 3 };
val arr2 : List[int] = reverse(arr);
val arr3 : List[int] = insert(arr, 4, 2);
val arr4 : List[int] = replace(arr, 4, 2);

if (!equals(arr, arr2) && !equals(arr, arr3) && !equals(arr, arr4) &&
    equals(arr, reverse(arr2)) && equals(head(arr3), arr4))
	2
else
	1
